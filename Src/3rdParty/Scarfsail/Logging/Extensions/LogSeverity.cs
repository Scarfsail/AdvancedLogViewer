namespace Scarfsail.Logging
{
    public enum LogSeverity
    {
        VERBOSE, /* the same as DEBUG.. now. */
        DEBUG,
        INFO,
        WARNING,
        FATAL,
        ERROR
    }
}
